"""
黄金价格监控工具 - 功能说明文档
================================

更新内容 (2026-03-19):
-------------------

1. ✅ 沪金价格显示单位：元/克
   - 位置：第一行（Ⓐ 图标）
   - 数据来源：京东金融 API
   - 显示格式：例如 "555.23"

2. ✅ 国际金价格显示单位：美元/盎司
   - 位置：第二行（Ⓖ 图标）
   - 数据来源：京东金融 API
   - 显示格式：例如 "2655.80"

3. ✅ 新增：国际金换算价格（元/克）
   - 位置：第三行（G¥ 图标，灰色斜体）
   - 计算公式：国际金价 (美元/盎司) × 汇率 ÷ 31.1035
   - 汇率来源：自动从多个 API 获取（缓存 5 分钟）
   - 备用汇率：7.2（当 API 失败时使用）
   - 显示格式：例如 "615.45"

技术实现细节:
-----------

### 1. 界面调整
- 卡片高度从 60px 增加到 80px（容纳三行显示）
- 新增 UI 元素：
  * intl_cny_label: "G¥" 标识（灰色斜体）
  * intl_cny_value_text: 换算后的人民币价格

### 2. 汇率获取函数 _fetch_exchange_rate()
- 优先级 1：阿里云市场 API
- 优先级 2：聚合数据 API
- 默认值：从 config.json 读取 default_exchange_rate（默认 7.2）
- 缓存机制：5 分钟内不重复请求

### 3. 价格换算逻辑
```python
# 国际金（美元/盎司）→ 人民币（元/克）
intl_cny = intl_usd * exchange_rate / 31.1035
# 其中：1 金衡盎司 = 31.1035 克
```

### 4. 数据线程更新
- 每次循环尝试更新汇率（受缓存保护）
- 同时获取三个数据点：
  * 沪金价格（元/克）
  * 国际金价格（美元/盎司）
  * 汇率（USD/CNY）

配置说明:
--------

在 config.json 中可添加以下配置：

```json
{
  "default_exchange_rate": 7.2,  // 默认汇率（API 失败时使用）
  "refresh_interval": 2,         // 刷新间隔（秒）
  "opacity": 0.55                // 透明度
}
```

使用示例:
-------

运行程序后，界面将显示三行信息：

```
Ⓐ 555.23  ↑        <- 沪金（元/克）
Ⓖ 2655.80 ↑        <- 国际金（美元/盎司）
G¥ 615.45          <- 国际金换算（元/克）
```

注意事项:
-------

1. 汇率 API 可能不稳定，程序已实现多源备份
2. 如果长期无法获取汇率，可手动修改 config.json 中的 default_exchange_rate
3. 第三行显示为灰色斜体，以区分直接价格和换算价格
4. 所有价格保留两位小数

作者：AI Assistant
日期：2026-03-19
"""

print(__doc__)
