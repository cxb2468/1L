# 快速开始指南

## 方式一：使用启动脚本（推荐）

双击运行 `start.bat`，脚本会自动：
1. 检查并安装依赖包
2. 初始化数据库
3. 启动Web服务器

启动后访问：http://localhost:8000

## 方式二：手动启动

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 初始化数据库

```bash
python init_db.py
```

### 3. 启动服务器

```bash
python main.py
```

或者使用uvicorn：

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## 登录系统

- 访问地址：http://localhost:8000
- 默认账号：admin
- 默认密码：123456

**请在首次登录后立即修改密码！**

## 功能模块

1. **学生管理**：学籍管理、转学/退学、毕业升级
2. **教员管理**：在职管理、退休/调离、档案管理
3. **成绩管理**：成绩录入、统计分析
4. **工资管理**：5张工资表、自定义项目
5. **班级管理**：班级设置、科目配置
6. **管理员设置**：权限管理、用户管理

## API文档

访问 http://localhost:8000/docs 查看完整的API文档和在线测试。

## 常见问题

### Q: 提示端口被占用？
A: 修改启动端口或关闭占用端口的程序
```bash
python main.py --port 8080
```

### Q: 如何重置数据库？
A: 删除 `school_mis.db` 文件后重新运行 `python init_db.py`

### Q: 忘记密码怎么办？
A: 删除 `school_mis.db` 文件后重新运行 `python init_db.py`，会重新创建默认管理员

## 技术支持

如有问题请查看：
- README_PYTHON.md - 详细文档
- http://localhost:8000/docs - API文档
